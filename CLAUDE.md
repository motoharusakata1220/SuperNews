# SuperNews — 自動ニッチ情報収集・動画生成ツール

---

## GitHub操作の絶対ルール（最優先）

以下の操作は**坂田さんから明示的に許可を得るまで絶対に行わない**：

- `git push`（いかなるブランチへも禁止）
- `gh pr create`（PRの作成禁止）
- `git merge` / `git rebase`（ブランチの統合禁止）
- `--no-verify` フラグの使用（フック回避禁止）

**許可される操作**（ローカルのみ）：
- `git add` / `git commit`（ローカルコミットのみ）
- `git checkout` / `git branch`（ブランチ切り替え・作成）
- `git log` / `git status` / `git diff`（確認のみ）
- テストの実行

**違反した場合**：即座に坂田さんに報告し、速やかに取り消す。

---

## プロジェクト概要

横断的知識を得ながら、カテゴリーごとに自動的にニュースにも乗らないニッチな情報を収集し、あわよくば動画を生成する自動化ツール。

---

## ディレクトリ構造

```
SuperNews/
├── CLAUDE.md                  # このファイル（プロジェクトルール）
├── .claude/settings.json      # Claude自動承認設定
├── scripts/                   # 管理スクリプト群（環境ルール）
│   ├── boot.sh                # 全カテゴリ一括起動（tmux + Claude）
│   ├── broadcast.sh           # 全ウィンドウへ一括指示送信
│   ├── delegate.sh            # 単一カテゴリへClaude委譲
│   ├── new-category.sh        # 新規カテゴリフォルダ作成
│   └── status.sh              # 全体の稼働状況確認
├── src/                       # ソースコード（環境ルール）
├── tests/                     # テストファイル（環境ルール）
├── 投資/                      # カテゴリフォルダ（日本語）
├── [カテゴリ名]/               # カテゴリフォルダは日本語で命名
└── README.md                  # プロジェクト概要
```

**命名規則**: 環境ルール用フォルダ（`scripts/`, `src/`, `tests/`, `.claude/`）以外は日本語名を使う。

---

## Claudeへの共通指示

- 日本語で回答すること
- ファイル生成前に構成を提示し確認を取ること
- 成果物は必ず `output/` フォルダに保存すること
- 不明な点は作業を止めて質問すること
- 機密情報を外部サービスに送信しないこと

---

## 開発ルール — TDD（テスト駆動開発）

全ての開発はRed -> Green -> Refactorサイクルで進める。

### サイクル

1. **Red**: 失敗するテストを先に書く
2. **Green**: テストが通る最小限の実装を書く
3. **Refactor**: テストが通ったままコードを整理する

### テストの原則

- テストファイルは実装ファイルと同階層に置く（例: `foo.ts` -> `foo.test.ts`）
- 1つのテストは1つの振る舞いだけを検証する
- テスト名は「〜のとき〜になる」の形式で日本語で書く
- モックは外部依存（API・DB・ファイルI/O）にのみ使用する。内部ロジックはモックしない
- テストカバレッジ目標: ビジネスロジック100%、UIコンポーネント主要パス100%

### テストコマンド

```bash
npm test              # 全テスト実行
npm test -- --watch   # ウォッチモード
npm run test:coverage # カバレッジレポート
```

---

## 開発ルール — ベストプラクティス

### コード品質

- **型安全**: TypeScriptのstrictモード必須。`any`禁止（外部ライブラリの型がない場合を除く）
- **命名**: 変数・関数は英語。UIテキスト・コメントは日本語
- **単一責任**: 1ファイル・1関数は1つの責務のみ
- **副作用の分離**: 純粋関数（ロジック）と副作用（I/O・API呼び出し）を明確に分ける
- **早期リターン**: ネストを減らすためガード節を使う

### Git運用

- コミットメッセージ: `feat:` / `fix:` / `refactor:` / `test:` / `docs:` のプレフィックスをつける
- 1コミット = 1つの論理的変更
- コミット前に必ずテストが通っていることを確認する

### ファイル・ディレクトリ構成

- 機能単位でディレクトリを切る（コロケーション）
- **フォルダ名は日本語にする**（環境ルール用フォルダ `scripts/`, `src/`, `.claude/` 等を除く）
- 設定ファイル（`.env`・シークレット）はリポジトリに含めない
- 生成物・ビルド成果物は `.gitignore` で除外する

### レビューチェックリスト（実装完了時に確認）

- [ ] 全テストがグリーン
- [ ] 型エラーなし（`tsc --noEmit` 通過）
- [ ] Lintエラーなし（`eslint` 通過）
- [ ] 不要な `console.log` が残っていない
- [ ] ハードコードされたシークレット・URLがない

---

## セキュリティルール

- 外部URLへのデータ送信は必ず確認を取る
- 個人情報を含むファイルは `output/` 外に出さない
- APIキー等のシークレットはコードにハードコードしない
