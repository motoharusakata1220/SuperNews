{
  "name": "background-knowledge",
  "version": "1.0.0",
  "description": "背景知識の構造化・集約モジュール",
  "scripts": {
    "build": "tsc",
    "aggregate": "node dist/index.js",
    "test": "jest --no-coverage",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.3.0"
  }
}
