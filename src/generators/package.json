{
  "name": "supernews-generators",
  "version": "1.0.0",
  "description": "YouTube動画自動生成パイプライン（全12フレームワーク統合）",
  "main": "dist/pipeline.js",
  "scripts": {
    "build": "tsc",
    "generate": "tsc && node dist/pipeline.js",
    "test": "jest --config jest.config.js",
    "test:watch": "jest --config jest.config.js --watch",
    "test:coverage": "jest --config jest.config.js --coverage"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.3.3"
  }
}
